interface:
  display_name: "Deck Builder"
  short_description: "Build reference-led decks with clear logic and visuals"
  default_prompt: "Use $deck-builder to turn this material into a finished presentation, inspect any supplied reference deck, explain the mechanisms visually, and verify the final file."
